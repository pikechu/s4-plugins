; CampaignCompletionDebug phase-2.1 diagnostic bootstrap settings.
; These values document the diagnostic policy; the current bootstrap keeps the
; same fail-closed defaults compiled in so that configuration cannot enable hooks.
[Diagnostic]
Version=0.2.1
LogLevel=Info
ModuleInventory=1
PublicListeners=1
InternalHooks=0
CompletionDetection=0
CompletionStorage=0
CompletionMarkers=0
ControlledStopFile=CampaignCompletionDebug.stop
